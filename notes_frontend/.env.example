# PUBLIC_INTERFACE
# REACT_APP_API_BASE_URL is used by the frontend to communicate with the backend API.
# Default (if not set) is http://localhost:3001 as a fallback in src/api.js.
# Set this value when deploying to a different environment or URL.
# Example:
# REACT_APP_API_BASE_URL=http://localhost:3001
REACT_APP_API_BASE_URL=
